---
name: lwt-handoff
description: Stub. Not yet implemented — planned to surface the repo's handoff protocol (see HANDOFF.md and docs/handoff-protocol.md, `scripts/lwt_handoff.py`) from inside a session. Use when the user asks lwt to read or write a handoff and get this stub in reply.
---

# lwt-handoff

Not yet implemented. This is a placeholder so the skill name and its
CI prefix check exist ahead of the feature. When invoked, say plainly
that `lwt-handoff` is a stub and point at `scripts/lwt_handoff.py`
(`--check` / `--write`) and `docs/handoff-protocol.md` for the current,
script-driven way to read or write a handoff.
