---
name: lwt-status
description: Stub. Not yet implemented — planned to report lwt's own install health (hook wired, policy readable, ledger writable) as a single healthy/unhealthy line, matching the statusline indicator from owner directive 6. Use when the user asks for lwt's status and get this stub in reply.
---

# lwt-status

Not yet implemented. This is a placeholder so the skill name and its
CI prefix check exist ahead of the feature. When invoked, say plainly
that `lwt-status` is a stub and point at `docs/architecture.md` for the
current state of the statusline indicator (owner directive 6).
