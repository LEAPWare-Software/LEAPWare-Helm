---
name: lwh-handoff
description: Stub. Not yet implemented — planned to surface the repo's handoff protocol (see HANDOFF.md and docs/handoff-protocol.md, `scripts/lwh_handoff.py`) from inside a session. Use when the user asks lwh to read or write a handoff and get this stub in reply.
---

# lwh-handoff

Not yet implemented. This is a placeholder so the skill name and its
CI prefix check exist ahead of the feature. When invoked, say plainly
that `lwh-handoff` is a stub and point at `scripts/lwh_handoff.py`
(`--check` / `--write`) and `docs/handoff-protocol.md` for the current,
script-driven way to read or write a handoff.
